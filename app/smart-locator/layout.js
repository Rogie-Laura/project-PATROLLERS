export const metadata = {
  title: "PRO4A — Patrollers Smart Locator",
  description: "Plot vital installations and establishments on the map",
};

export default function SmartLocatorLayout({ children }) {
  return children;
}
